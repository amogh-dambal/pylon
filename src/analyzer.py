# python 3.6
# basic data analysis functions
import pandas as pd


def filter(pbp: pd.DataFrame, **kwargs: dict) -> pd.DataFrame:
	pass


def query(pbp: pd.DataFrame, **kwargs: dict) -> pd.DataFrame:
	pass